Project-Root/
├── .gitignore                # 전체 프로젝트 Git 제외 설정
├── README.md                 # 전체 프로젝트 통합 가이드
├── project.code-workspace    # VS Code 워크스페이스 설정 파일
│
├── frontend/                 # [React] 프론트엔드
│   ├── public/               # 정적 자산
│   ├── src/                  # React 소스 코드 (Components, Hooks, Pages 등)
│   ├── package.json          # 의존성 관리
│   └── README.md             # 프론트 실행 방법
│
├── backend-spring/           # [Spring Boot] 메인 백엔드 (비즈니스 로직, DB 연동)
│   ├── src/main/java/        # Java 소스 코드
│   ├── src/main/resources/   # 설정 파일 (application.yml)
│   ├── build.gradle          # Gradle 빌드 설정
│   └── README.md             # Java 서버 실행 방법
│
├── backend-ai/               # [FastAPI] AI 서빙 서버 (추론용 API)
│   ├── .venv/                # FastAPI 전용 가상환경
│   ├── app/                  # API 로직 (main.py, routers)
│   ├── core/                 # 모델 로드 및 전처리 로직
│   ├── requirements.txt      # 가볍게 유지 (FastAPI, Uvicorn, 모델 라이브러리)
│   └── README.md             # AI API 실행 방법
│
├── ai-engine/                # [Python] AI 모델 학습 및 연구
│   ├── .venv/                # 학습/연구용 가상환경 (Heavy 패키지)
│   ├── notebooks/            # Jupyter Notebook (.ipynb)
│   ├── data/                 # 데이터셋 (Raw, Processed)
│   ├── models/               # 학습 완료된 모델 저장소 (.h5, .pth 등)
│   ├── train.py              # 학습 스크립트
│   └── requirements.txt      # 학습용 라이브러리 (TensorFlow, PyTorch, Pandas 등)
│
├── database/                 # [MariaDB] 데이터베이스 관리
│   ├── ddl/                  # 테이블 생성 SQL (Schema)
│   ├── dml/                  # 초기 데이터 및 테스트 데이터 SQL
│   └── erd/                  # ERD 설계 이미지 및 관련 문서
│
└── docs/                     # [Document] 기획 및 설계 문서
    ├── architecture/         # 시스템 구조도
    └── api-spec/             # API 명세서 (Swagger, Notion 링크 등)


[frontend 폴더]
# 1. react 설치
npm create vite@latest . -- --template react
- remove and existing 선택
- npm install 까지 설치해서 포함


# 가상환경 활성화 (Windows 기준)
.venv\Scripts\activate
python -m venv .venv

# 필수 라이브러리 설치
pip install fastapi uvicorn pydantic python-dotenv python-multipart

# AI 추론 및 이미지 처리 (YOLOv8 포함)
pip install ultralytics torch torchvision --index-url https://download.pytorch.org/whl/cpu
pip install opencv-python numpy

# LLM 및 RAG 관련 (Langchain, Vector DB, API)
pip install langchain langchain-openai langchain-google-genai langchain-community
pip install faiss-cpu chromaDB 

# AWS 및 클라우드 서비스(일단 제외하고 설치)
pip install boto3  

# 데이터 분석 및 시각화
pip install pandas numpy matplotlib seaborn tqdm

# 머신러닝 및 이미지 처리
pip install scikit-learn opencv-python pillow joblib

# 딥러닝 학습 (YOLOv8 & PyTorch)
# ※ NVIDIA GPU(CUDA)가 있다면 공식 사이트 가이드를 따라 설치하는 것이 좋음
pip install ultralytics torch torchvision torchaudio

# 주피터 노트북 사용을 위한 커널 설치
pip install ipykernel